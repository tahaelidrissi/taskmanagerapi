version: "3"

services:
  app:
    image: tahaidrissi05/taskmanager_api:latest
    ports:
      - "8000:8000"              
    env_file:
      - .env                             
    restart: unless-stopped                

